# Point this at your own Postgres server.
# If it requires SSL (RDS, Supabase, Neon, Azure, etc.) append ?sslmode=require
DATABASE_URL=postgresql+psycopg2://username:password@host:port/database_name


